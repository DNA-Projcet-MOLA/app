import { style } from "@vanilla-extract/css";

export const halfOpacity = style({
	opacity: 0.5,
});

export const fullHeight = style({
	height: "100%",
});

export const fullViewportHeight = style({
	height: "100vh",
});
